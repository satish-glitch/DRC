<apex:page renderAs="pdf" controller="DRC_NBC_GenerateSamplePiController" showHeader="false" sidebar="false" applyBodyTag="false" applyHtmlTag="false" standardStylesheets="false">
    <html>
        <head>
            <style>
                @page {
                    size: A4;
                    margin-top: 55mm;
                    margin-bottom: 30mm;
                    margin-left: 15mm;
                    margin-right: 15mm;
                    @top-center {
                        content: element(pageHeader);
                        width: 100%;
                    }
                    @bottom-center {
                        content: element(pageFooter);
                        width: 100%;
                    }
                }

                body {
                    font-family: Arial, sans-serif;
                    font-size: 9pt;
                    color: #000;
                    line-height: 1.3;
                }

                /* ══════════════════════════════════════
                   REPEATING HEADER
                ══════════════════════════════════════ */
                #pageHeader {
                    position: running(pageHeader);
                    width: 100%;
                }

                table.header-table {
                    width: 100%;
                    border-collapse: collapse;
                    table-layout: fixed;
                }

                td.hcol-title {
                    vertical-align: top;
                    padding: 0;
                    width: 55%;
                }
                .oc-title {
                    font-size: 20pt;
                    font-weight: bold;
                    line-height: 1.15;
                    margin: 0;
                    padding: 0;
                }
                .oc-orderno {
                    font-size: 16pt;
                    font-weight: bold;
                    margin: 2px 0 0 0;
                    padding: 0;
                }
                .oc-date,
                .oc-page {
                    font-size: 9pt;
                    font-weight: normal;
                    margin: 2px 0 0 0;
                    padding: 0;
                }

                td.hcol-addr-logo {
                    vertical-align: top;
                    text-align: center;
                    padding: 0 0 0 8px;
                    width: 45%;
                }
                td.hcol-addr-logo p,
                td.hcol-addr-logo div {
                    margin: 1px 0;
                    padding: 0;
                    font-size: 7.5pt;
                }
                td.hcol-addr-logo h1,
                td.hcol-addr-logo .company-name {
                    font-size: 12pt;
                    font-weight: bold;
                    margin: 0 0 2px 0;
                    padding: 0;
                }

                .logo-wrapper {
                    text-align: right;
                    margin-bottom: 4px;
                }
                .company-logo {
                    width: 72px;
                    height: auto;
                }

                .header-divider {
                    border-top: 1.5px solid #000;
                    margin-top: 6px;
                }

                /* ══════════════════════════════════════
                   REPEATING FOOTER
                ══════════════════════════════════════ */
                #pageFooter {
                    position: running(pageFooter);
                    width: 100%;
                    border-top: 1px solid #000;
                    padding-top: 4px;
                    font-size: 7.5pt;
                    color: #000;
                    line-height: 1.5;
                }

                table.footer-table {
                    width: 100%;
                    border-collapse: collapse;
                    font-size: 7.5pt;
                }
                table.footer-table td {
                    padding: 1px 4px;
                    vertical-align: top;
                }
                .footer-label {
                    color: #555;
                    font-size: 7pt;
                }
                .footer-value {
                    font-size: 7.5pt;
                    font-weight: bold;
                }

                /* ── CUSTOMER / ADDRESS BLOCK ── */
                .address-section {
                    width: 100%;
                    margin-top: 6px;
                    margin-bottom: 4px;
                }
                table.address-table {
                    width: 100%;
                    border-collapse: collapse;
                    font-size: 9pt;
                }
                table.address-table td {
                    vertical-align: top;
                    padding: 1px 0;
                }
                .addr-left {
                    width: 65%;
                }
                .addr-right {
                    width: 35%;
                    text-align: right;
                }
                .addr-name {
                    font-weight: bold;
                    font-size: 9pt;
                }

                /* ── ORDER DETAIL COLUMNS ── */
                .detail-section {
                    width: 100%;
                    margin-top: 10px;
                    margin-bottom: 4px;
                }
                table.detail-table {
                    width: 100%;
                    border-collapse: collapse;
                    font-size: 8pt;
                }
                table.detail-table td {
                    vertical-align: top;
                    padding: 1px 0;
                }
                .detail-left  { width: 50%; }
                .detail-right { width: 50%; }

                table.kv-table {
                    width: 100%;
                    border-collapse: collapse;
                    font-size: 8pt;
                    line-height: 1.3;
                }
                table.kv-table td {
                    padding: 1px 2px;
                    vertical-align: top;
                }
                td.kv-label {
                    font-weight: bold;
                    width: 50%;
                    white-space: nowrap;
                }
                td.kv-value {
                    width: 50%;
                    text-align: left;
                    word-break: break-word;
                }
                td.kv-value-right {
                    width: 50%;
                    text-align: right;
                    word-break: break-word;
                }

                /* ── META ROW ── */
                .meta-divider {
                    border-top: 1px solid #ccc;
                    margin: 8px 0 4px 0;
                }
                table.meta-table {
                    width: 100%;
                    border-collapse: collapse;
                    font-size: 8pt;
                    margin-bottom: 8px;
                }
                table.meta-table td {
                    vertical-align: top;
                    padding: 2px 4px 2px 0;
                }
                .meta-label {
                    font-weight: bold;
                    font-size: 7.5pt;
                    display: block;
                }
                .meta-value {
                    font-size: 9pt;
                    display: block;
                }

                /* ── ITEMS TABLE ── */
                .items-table {
                    width: 100%;
                    border-collapse: collapse;
                    margin-bottom: 10px;
                    margin-top: 5px;
                    font-size: 8pt;
                }
                .items-table th {
                    border-top: 1px solid #000;
                    border-bottom: 1px solid #000;
                    padding: 4px 6px;
                    text-align: center;
                    font-weight: bold;
                    font-size: 8pt;
                }
                .items-table td {
                    border: none;
                    padding: 4px 6px;
                }
                .items-table td.right  { text-align: right; }
                .items-table td.center { text-align: center; }
                .items-table td.left   { text-align: left; }

                /* ── TOTALS ── */
                .totals-table {
                    width: 45%;
                    float: right;
                    border-collapse: collapse;
                    font-size: 8.5pt;
                    margin-top: 8px;
                }
                .totals-table td {
                    padding: 3px 6px;
                }
                .totals-table td.t-label {
                    font-weight: bold;
                    text-align: left;
                }
                .totals-table td.t-value {
                    text-align: right;
                    font-weight: bold;
                }
                .totals-table tr.total-line td {
                    border-top: 1px solid #000;
                    border-bottom: 1px solid #000;
                }

                /* ── TERMS ── */
                .terms-section {
                    clear: both;
                    margin-top: 18px;
                    font-size: 7pt;
                    page-break-inside: avoid;
                }
                .terms-section h3 {
                    font-size: 8.5pt;
                    margin: 8px 0 4px 0;
                    font-weight: bold;
                }
                .terms-section p {
                    margin: 2px 0;
                    text-align: justify;
                }
            </style>
        </head>
        <body>

            <!-- ═══ REPEATING HEADER ═══ -->
            <div id="pageHeader">
                <table class="header-table">
                    <tr>
                        <!-- Col 1: Title Block -->
                        <td class="hcol-title">
                            <p class="oc-title">Order Confirmation</p>
                            <p class="oc-orderno">{!importSequence}</p>
                            <p class="oc-date">
                                <apex:outputText value="{0,date,dd MMMM yyyy}">
                                    <apex:param value="{!orderDate}" />
                                </apex:outputText>
                            </p>
                            <p class="oc-page">Page 1 / 1</p>
                        </td>

                        <!-- Col 2: Logo -->
                        <td class="hcol-addr-logo">
                            <div class="logo-wrapper">
                                <apex:image url="{!URLFOR($Resource.DRC_NBC_DR_Logo)}" styleClass="company-logo" />
                            </div>
                        </td>
                    </tr>
                </table>
                <div class="header-divider"></div>
            </div>

            <!-- ═══ REPEATING FOOTER ═══ -->
            <div id="pageFooter">
                <table class="footer-table">
                    <tr>
                        <td style="width:25%; text-align:center;">
                            <span class="footer-label">Home Page</span>
                            <span class="footer-value">{!website}</span>
                        </td>
                        <td style="width:25%; text-align:center;">
                            <span class="footer-label">Phone No.</span>
                            <span class="footer-value">{!phoneNumber}</span>
                        </td>
                        <td style="width:50%; text-align:left;">
                            <span class="footer-label">Email</span>
                            <span class="footer-value">{!email}</span>
                        </td>
                    </tr>
                    <tr>
                        <td>
                            <span class="footer-label">{!Bank}</span><br/>
                            <span class="footer-value">{!AccountNo}</span>
                        </td>
                        <td>
                            <span class="footer-label">IBAN</span>
                        </td>
                        <td>
                            <span class="footer-label">SWIFT Code</span><br/>
                            <span class="footer-value">ICICINBBCTS</span>
                        </td>
                    </tr>
                </table>
            </div>

            <!-- ═══ CUSTOMER ADDRESS BLOCK ═══ -->
            <div class="address-section">
                <table class="address-table">
                    <tr>
                        <!-- Left: Shipping address -->
                        <td class="addr-left">
                            <span class="addr-name">{!customerName}</span><br/>
                            {!shippingAddress}<br/>
                            {!shippingCity}<apex:outputText value=", {!shippingState}" rendered="{!NOT(ISBLANK(shippingState))}"/><br/>
                            {!shippingPostalCode}<br/>
                            {!shippingCountry}
                        </td>
                        <!-- Right: Salesperson + Country -->
                        <td class="addr-right">
                            {!SalesPerson}<br/>
                            {!billingCountry}
                        </td>
                    </tr>
                </table>
            </div>

            <!-- ═══ META ROW ═══ -->
            <div class="meta-divider"></div>
            <table class="meta-table">
                <tr>
                    <td style="width:25%;">
                        <span class="meta-label">External Document No.</span>
                        <span class="meta-value">{!PO}</span>
                    </td>
                    <td style="width:25%;">
                        <span class="meta-label">Salesperson</span>
                        <span class="meta-value">{!SalesPerson}</span>
                    </td>
                    <td style="width:25%;">
                        <span class="meta-label">Quote No.</span>
                        <span class="meta-value">&#160;</span>
                    </td>
                    <td style="width:25%;">
                        <span class="meta-label">Shipment Method</span>
                        <span class="meta-value">{!shipmentMethod}</span>
                    </td>
                </tr>
            </table>

            <!-- ═══ ITEMS TABLE ═══
                 GST % column is shown ONLY when billing country is India
            -->
            <table class="items-table">
                <thead>
                    <tr>
                        <!-- Width redistributed: when GST hidden, remaining columns share the 10% -->
                        <th style="text-align:center;">No.</th>
                        <th style="text-align:left;">Item Ref No.</th>
                        <th style="text-align:left;">Description</th>
                        <th style="text-align:center;">Quantity</th>
                        <apex:outputText rendered="{!isIndianBilling}">
                            <th style="text-align:center;">Unit Price Excl. GST</th>
                        </apex:outputText>
                        <apex:outputText rendered="{!NOT(isIndianBilling)}">
                            <th style="text-align:center;">Unit Price</th>
                        </apex:outputText>
                        <!-- GST % column: visible only for India billing -->
                        <apex:outputText rendered="{!isIndianBilling}">
                            <th style="text-align:center;">GST </th>
                        </apex:outputText>
                        <apex:outputText rendered="{!isIndianBilling}">
                            <th style="text-align:right;">Line Amount Excl. GST</th>
                        </apex:outputText>
                        <apex:outputText rendered="{!NOT(isIndianBilling)}">
                            <th style="text-align:right;">Line Amount</th>
                        </apex:outputText>
                    </tr>
                </thead>
                <tbody>
                    <apex:variable var="rowNum" value="{!1}" />
                    <apex:repeat value="{!items}" var="item">
                        <tr>
                            <td class="center">{!rowNum}</td>
                            <td class="left">{!item.customerProductCode}</td>
                            <td class="left">{!item.Description}</td>
                            <td class="center">{!item.quantity}</td>
                            <td class="center">{!item.unitPrice}</td>
                            <!-- GST % cell: visible only for India billing -->
                            <apex:outputText rendered="{!isIndianBilling}">
                                <td class="center">{!item.ItemGST}</td>
                            </apex:outputText>
                            <td class="right">{!item.amount}</td>
                        </tr>
                        <apex:variable var="rowNum" value="{!rowNum + 1}" />
                    </apex:repeat>
                </tbody>
            </table>

            <!-- ═══ TOTALS ═══
                 Label uses dynamic currency code from Order
            -->
            <table class="totals-table">
                <tr class="total-line">
                    <td class="t-label">
                        Total {!currencyCode}
                        <!-- Show "Incl. GST" only for Indian billing -->
                        <apex:outputText value=" Incl. GST" rendered="{!isIndianBilling}" />
                    </td>
                    <td class="t-value">{!totalWithTax}</td>
                </tr>
            </table>

            <div style="clear:both;"></div>

        </body>
    </html>
</apex:page>